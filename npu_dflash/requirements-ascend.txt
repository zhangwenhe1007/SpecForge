# Reproducible Ascend NPU install for SpecForge DFlash.
#
# Usage:
#   python -m pip install -r npu_dflash/requirements-ascend.txt
#
# Notes:
# - The Huawei Cloud mirror is used for torch, torchvision, torch_npu, and
#   normal PyPI packages.
# - SpecForge's upstream pyproject currently pins torch==2.9.1. Ascend NPU
#   support is pinned here to torch/torch_npu 2.9.0 instead.
# - Install SpecForge itself with `pip install -e . --no-deps` after this file.
# - Install SGLang from source with its NPU pyproject; see install_npu_env.sh.

--index-url https://mirrors.huaweicloud.com/repository/pypi/simple/
--trusted-host mirrors.huaweicloud.com

torch==2.9.0
torchvision==0.24.0
torch_npu==2.9.0

yunchang==0.6.4

transformers==4.57.1
qwen-vl-utils==0.0.11

datasets==4.8.5
accelerate==1.13.0
safetensors==0.7.0
einops==0.8.2

numpy<2.0
pydantic==2.13.3
psutil
tqdm
ninja
packaging
setuptools<81

decorator
cloudpickle
ml-dtypes
tornado
scipy
cffi
pybind11
sympy
absl-py
protobuf>=4.25,<5

wandb==0.26.1
tensorboard==2.20.0

openai-harmony==0.0.8

# SGLang runtime dependencies installed explicitly because SGLang is installed
# editable with --no-deps to preserve the Ascend torch/torch_npu/transformers
# pins above.
IPython
aiohttp
anthropic>=0.20.0
blobfile==3.0.0
av
build
compressed-tensors
fastapi
easydict
gguf
hf_transfer
huggingface_hub
interegular
llguidance>=0.7.11,<0.8.0
modelscope
msgspec
openai==2.6.1
orjson
outlines==0.1.11
partial_json_parser
pillow
prometheus-client>=0.20.0
py-spy
pybase64
python-multipart
pyzmq>=25.1.2
sentencepiece
setproctitle
soundfile==0.13.1
tiktoken
timm==1.0.16
torchao==0.9.0
uvicorn>=0.24.0,<1
uvloop
xgrammar==0.2.0
smg-grpc-servicer>=0.5.0
mistral_common>=1.11.0
